function solution(array, height) {
  const result = array.filter((v) => v > height).length;

  return result;
}
function solution(array, height) {
  const result = array.filter((v) => v > height).length;

  return result;
}
